using '../main.bicep'

param environmentName = 'dev'
param location = 'eastus'  // or your region
param aksClusterName = 'your-aks-cluster-name'
param aksResourceGroupName = 'your-aks-rg'  // if different
param keyVaultName = 'your-keyvault-name'
param tags = {
  environment: 'dev'
  managedBy: 'bicep'
  application: 'wiz-connector'
  costCenter: 'security'
}
