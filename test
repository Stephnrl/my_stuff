apiVersion: v1
kind: ServiceAccount
metadata:
  name: wiz-service-account
  namespace: wiz
  annotations:
    azure.workload.identity/client-id: "<IDENTITY_CLIENT_ID>"  # Replace with actual client ID
  labels:
    azure.workload.identity/use: "true"
