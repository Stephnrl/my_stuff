module "ansible_configuration" {
  source = "./modules/ansible-runner"
  
  # ... other variables
  
  galaxy_requirements_yml = yamlencode({
    collections = [
      {
        name    = "community.general"
        version = ">=7.0.0"
      },
      {
        name    = "ansible.posix"
        version = ">=1.5.0"
      }
    ]
    roles = [
      {
        name    = "geerlingguy.docker"
        version = "7.0.1"
      }
    ]
  })
}
